# Qualification: TTS‑0005 revision 16

- Candidate: existing TTS‑0005 revision 16 at `7cdf7c4fc6b78599d5a3edd71bb1400844fb8dc0`
- Publication date: 2026‑08‑23
- Qualified document SHA‑256: `b61f3ccd7863cc701aab6da169fa75b947f3047d292ed142a716b4edbf36dee1`
- Evidence commit: `53c3e60149985b9c91f0942033b95a91fed8aeec`
- TPS revision: TPS‑0001 revision 2, SHA‑256 `3e52cba974d025fafc7a0b0dfbe763824c238e14dd7ec0a04f865ec4409c59f6`
- Approval: Stephen Hurd, 2026‑08‑23

TTS‑0005 revision 16 is an existing standard undergoing its first
qualification under TPS‑0001. It is not a TSP promotion, and its identifier
and revision do not change when this record is published.

## Normative dependencies

| Publication | Revision | SHA‑256 or exact external version |
| --- | ---: | --- |
| TTS‑0002 | 3 | `b073ecb328a67a2477db49e7a3f8d1e3da5c5c05caf0709d398fc1006a906dbb` |
| TTS‑0003 | 7 | `1e1e8d25a5d819568c1f18f70b161ad0c816196f9e6b62d18181fffd144200be` |
| TTS‑0004 | 5 | `abede4c5e0d559ff0dd57967f5e6aefc9aabd380d20ec17924e0fcb63e25a7b6` |
| TTS‑0008 | 2 | `d4aa55939c6b95a6cbef1e7994b6f169c5f98ade8ea872614508babad7dfba8f` |
| TTS‑0009 | 2 | `bab865d88e53da1c40f3c27468d5e5cfcc825ae94998b6ef42fb0c29f007ac36` |
| FTA‑1006 | 2 | `2265e8410311850f4be3068aed446876e017d45d70dd3d41bb3c3991da6f20e4` for `fta-1006.002` |
| Libhydrogen, “Generic hashing” | Version 1 | Git commit `705149f9c8db1f400775477adb6f9c2254103dca`, file `Generic-hashing.md`, SHA‑256 `2c92f39762b726edfc776f8cde0d64f772afe03d85d19fa5d1e6880c760a3ce3` |

The complete internal graph is:

```text
TTS‑0005 → TTS‑0002, TTS‑0003, TTS‑0004, TTS‑0008, TTS‑0009
TTS‑0003 → TTS‑0002, TTS‑0004
TTS‑0008 → TTS‑0004
TTS‑0009 → TTS‑0002, TTS‑0003, TTS‑0004, TTS‑0007
TTS‑0007 → TTS‑0002
```

TTS‑0002 and TTS‑0004 have no internal normative dependency. Every edge points
toward an already qualified lower layer, and the graph is acyclic. TPS‑0001 is
a process specification and is not part of the technical graph.

## Conformance roles and features

| Role | Mandatory features | Optional or conditional features |
| --- | --- | --- |
| Bundle producer | Canonical Origin, Header, payload hashes, SignedTLVs, assigned item carriers, and exact identity/key placement | Multiple payload SignedTLVs, permitted unknown values, and experimental types 32–63 |
| Bundle consumer | Exact leading-child grammar, signature verification, header correlation, assigned-type rejection, and framing/data disposition | Unknown-value retention and explicitly agreed experimental values |
| Request producer and reply consumer | Directional identifiers, exact payload hash correlation, reversed identities, one authenticated response per request, and completion without FIN | First-contact and predecessor continuity probes |
| Reply producer | Accepted/Rejected grammar, independent request outcomes, returned-value placement, and current transmission correlation | Optional temporary-retry Timestamp and description; retained predecessor secret |
| Poll holder/server | Complete atomic snapshot scoped to exact authenticated identity and requested kind | Empty snapshots and anonymous identities |
| Acceptance store | Store item and duplicate identity durably before Accepted; suppress accepted duplicates indefinitely | Explicit administrative removal of a retained identity |
| Key-continuity server | Dedicated reply grammar, predecessor signing when available, current-key permanent refusal otherwise | Retained predecessor keys and authorized first contact |

The recommendations about payload grouping are implemented without imposing a
parser limit: producers can build the recommended one- or two-payload layout,
and consumers accept any number of valid payload SignedTLVs. Every named
optional or conditional path above is implemented.

## Requirement matrix

`C` denotes complete coverage of the owning source item in the coverage table
below. Qualified dependency behavior is identified by its TTS number.

| Requirement | Role | Implementation | Positive evidence | Negative evidence | Coverage |
| --- | --- | --- | --- | --- | --- |
| §2 Bundle begins literally with Origin and carries an adjacent key exactly for anonymous Origin | Producer/consumer | `bundle.rs::parse_internal`, `build_bundle` | `anonymous_keys_are_carried_and_bad_signatures_are_rejected`, round trips | `unknown_value_cannot_precede_bundle_origin`; header/boundary rejection cases | C; TTS‑0003/0004 |
| §2 non-anonymous PublicKey is prohibited except for the dedicated reply and never overrides effective trust | Consumer | `bundle.rs` identity and probe-reply classification | `predecessor_key_certifies_a_successor_in_a_probe_reply` | exact-reply-shape and bad-signature cases; TTS‑0008 no-fallback witnesses | C; TTS‑0008 |
| §2 Header is the next defined SignedTLV, has no Origin, and contains ordered Destination, conditional key, and Timestamp | Producer/consumer | `validate_header`, Bundle builders | non-anonymous, anonymous, predecessor, and first-contact round trips | `bundle_parser_rejects_each_header_and_payload_boundary`; separated-key and cardinality cases | C; TTS‑0003 |
| §2 each payload begins literally with the exact encoded Header SignedTLV hash | Producer/consumer | `hash_tlv`, `build_signed_tlv`, payload parser | `non_anonymous_bundle_round_trip_and_exact_hash` | `payload_hash_is_the_literal_first_child` covers missing, displaced, and wrong hash | C |
| §2 unknown children preserve defined ordering and bytes; assigned children in prohibited positions are errors | Consumer | Bundle parser and `item_carrier.rs` classifiers | Bundle/header unknown-retention tests | carrier-helper and bundle-boundary assigned-type failures | C |
| §2 experimental values 32–63 and arbitrary valid payload counts remain representable | Producer/consumer | generic `OwnedTlv` payload construction and unknown carrier classification | carrier helper tests exercise non-request opaque values and multiple payload accounting | assigned type 100 and malformed assigned values remain errors | C |
| §2 recommended grouping does not create a parser limit or whole-Bundle buffer rule | Producer/consumer | streaming SignedTLV framing plus vector of completed payloads | one-, two-, and distinct-payload tests | completion tests prove accounting is per authenticated SignedTLV | C plus integration |
| §3 types 64–66 carry exactly the TTS‑0009 Message, File, and FileRequest sequences | Item carrier | `validate_item` through `item_carrier.rs` | complete TTS‑0009 item suite and carrier helper tests | malformed and wrong carrier/item combinations fail | C; TTS‑0009 |
| §3 Rejected has identifier, hash, reason, conditional Timestamp, and optional UTF‑8 description | Reply producer/consumer | `rejected`, `validate_response` | `rejected_response_parser_covers_every_reason_boundary` covers all reasons, timestamp states, and descriptions | same test rejects invalid reason, noncanonical integer, malformed UTF‑8, trailing form, and prohibited Timestamp | C |
| §3 Accepted has identifier and hash, with exactly one key only for PublicKeyRequest | Reply producer/consumer | `accepted`, `validate_response` | carrier and response-accounting tests cover ordinary and key Accepted | key on ordinary response, missing key, duplicate key, and malformed children are rejected | C |
| §3 Poll requests have exactly one identifier and the three kinds retain distinct semantics | Poll requester/server | `simple_request`, `poll_kinds`, `RequestKind` | carrier helpers and `every_item_and_request_kind_has_one_accounting_class` | extra/missing/malformed identifier cases | C |
| §3 PublicKeyRequest is sole request in the sole exact payload | Key requester/server | `validate_public_key_request`, Bundle parser/builders | dedicated predecessor and first-contact probe tests | `public_key_request_is_rejected_when_mixed_or_repeated` and exact payload-shape cases | C |
| §3 available requested predecessor signs only the dedicated reply; unavailable predecessor produces reason 1 under current key | Key server | `public_key_response.rs::build`, reply builders | retained/current predecessor tests | `unavailable_predecessor_reply_is_signed_by_the_current_key`; malformed/refused shape cases | C plus live daemon tests |
| §3 Poll atomically claims every matching unclaimed held value, including an empty set, and excludes later/active values | Poll store/server | `poll_snapshot.rs::claim_poll_snapshot` | `a_poll_snapshot_takes_every_matching_copy_and_ignores_mode_and_retry` | second claim is empty; active, wrong-kind, and later values are excluded | C |
| §3 Poll matching uses exact authenticated anonymous address/key identity and retains local key metadata | Poll store/server | snapshot key comparison and outbound delivery records | `a_snapshot_is_scoped_to_the_authenticated_identity_not_just_the_address` | same address with absent or different key does not match | C; TTS‑0004 |
| §4 request identifiers are distinct per payload/direction and responses repeat identifier plus exact payload hash | Request/reply accounting | `ResponseTracker`, item carrier helpers | unordered and reverse-direction namespace tests | duplicate identifier, wrong identifier, wrong hash, and replay across distinct payloads fail | C |
| §4 TLVHash is the exact 32-byte unkeyed `HashTLV\0` hash of encoded SignedTLV bytes | Producer/consumer | `hash.rs` streaming wrapper and `hash_tlv` | `tlv_hash_is_stream_independent`, exact bundle hash tests | distinct contexts and changed encoded bytes produce different results; wrong length rejected | C |
| §5 framing failure stops at its offset; SignedTLV/data failure discards only the applicable SignedTLV | Consumer | qualified TLV/SignedTLV reader and Bundle error mapping | valid prefix and later payload tests | truncated, invalid integer, nested length, header, signature, and payload boundary tests | C; TTS‑0002/0003 |
| §5 permitted unknown TLVs are retained byte-for-byte and mixed Rejected suffix is not reinterpreted | Consumer | Bundle raw encodings and response parser | Bundle/header retention and UTF‑8 description cases | assigned-child misuse and malformed raw suffix fail | C |
| §6 Reply reverses exact transport identities, including anonymous keys, and answers exactly one Bundle | Reply producer/consumer | `ServerReply`, `ResponseTracker::observe_reply` | live exchange and unordered response tests | wrong Origin/Destination and unrelated response hash fail | C |
| §6 every request gets exactly one independent Accepted or Rejected in any order | Reply producer/consumer | request-kind accounting and completed set | `tracks_unordered_responses_by_signed_tlv_hash_and_identifier` | incomplete, duplicate, unexpected, and response-only cases | C |
| §6 continuity proof validates requested signer, reversed identities, identifier, hash, dedicated grammar, and successor | Key requester | Bundle probe parser and tracker | `predecessor_key_certifies_a_successor_in_a_probe_reply` | wrong signer/key/hash/id/shape and unavailable predecessor produce no continuity evidence | C; TTS‑0008 |
| §6 first-contact reply requires advertised Origin key and Accepted key to be the same current key | Key requester/server | Bundle first-contact classification/builders | `probe_builders_cover_first_contact_and_anonymous_requesters` | `first_contact_requires_the_advertised_and_accepted_keys_to_match` | C |
| §6 returned values precede and share the SignedTLV with Accepted; zero or more Files are allowed | Reply producer/consumer | payload validation and response tracker | live poll and reverse-order returned-value tests | final-reply missing/duplicate response and carrier-boundary cases | C plus live daemon tests |
| §6 completion occurs only after each enclosing SignedTLV is authenticated and all expected responses are complete | Reply consumer | `ResponseTracker::require_complete`, session state | `final_reply_completes_without_waiting_past_the_last_response` | `final_reply_must_answer_every_returned_value` and duplicate-response test | C |
| §7 first acceptance atomically makes payload and stable identity durable before Accepted | Acceptance store/server | `inbound_accept.rs::accept`, redb transaction | `signed_item_acceptance_is_durable_and_idempotent`; live acceptance | invalid carrier/authentication/identity combinations and store failure do not acknowledge | C |
| §7 accepted duplicate is not stored/imported/forwarded again and is Accepted using current correlation | Acceptance store/server | duplicate table lookup and current response construction | signed duplicate and live `accepts_once_and_acknowledges_a_signed_file_duplicate` | different signature/signer remains distinct; invalid identity cannot enter table | C |
| §7 identities do not expire automatically; explicit administration may remove one | Acceptance store | `remove_duplicate_identity` | durable reopen and explicit removal permit later new acceptance | second removal reports absent; ordinary processing has no expiry path | C |
| §7 embedded Files share Message identity; Poll/FileRequest do not use signed-item duplicate identity | Acceptance store | carrier/authentication validation and TTS‑0009 identity | Message/File/FileRequest carrier-combination tests | identity on FileRequest, unsigned/invalid item, or mismatched kind is rejected | C; TTS‑0009 |

The compound protocol decisions were also reviewed for condition independence.
The header/public-key matrix, dedicated-request matrix, Rejected reason and
Timestamp matrix, carrier/authentication matrix, snapshot identity/kind/state
matrix, and response hash/identifier/completion matrix each toggle every
independently effective condition while holding the others fixed. Together
with complete measured branches, this supplies the decision-condition evidence
requested by issue #114; TPS‑0001 itself requires complete function, line or
region, and branch coverage rather than a particular MC/DC tool.

## Conformance-surface manifest

| Source item | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-crypto/src/hash.rs` | `f4759818ced5a6a117f69b48672606aab6492adeb4768f1939dcbc3a6d539145` | §4 exact TLVHash and durable item hash wrapper |
| `crates/tith-wire/src/bundle.rs` | `34bd9b2798288641945595cbc3f1310c68164baad9e939c0949218f891609b04` | §§2, 5, and 6 Bundle/header/payload/probe grammar and builders |
| `crates/tith-wire/src/bundle_types.rs` | `701ce8edf6219c9b93c37bbbd95136b39441df656e902c7e0196ed9d5535deaf` | §§2–4 exact assignments and request classification |
| `crates/tith-wire/src/item_carrier.rs` | `37c257b5881895b177639680a902ace909007bdc435b2e34ba8df285efdd7dc8` | §§3–6 carrier request/response grammar |
| `crates/tith-exchange/src/response.rs` | `71e8afe69af37b1f0212320fac9ca1c516f31ff8c9eecf563eff6fd32e78f668` | §§4 and 6 outstanding-response accounting and Reply construction |
| `crates/tith-store/src/duplicate_identity.rs` | `3114e3ab789d83cd5b0b0dc09cc61c2e8d68b3b2ccf3fca98c31c5dbe4e8020a` | §7 durable identity encoding and administration |
| `crates/tith-store/src/inbound_accept.rs` | `a9216ee2180c9b959847b7ed3265cfb88c55d3cb86388005a6474f26230a269d` | §7 atomic acceptance and duplicate suppression |
| `crates/tith-store/src/poll_snapshot.rs` | `f14eb1677ec6bbcff17a29532cd9d097075a3c8e4d46f19aecda2c3c14e2d768` | §3 atomic complete Poll snapshot |
| `crates/tithd/src/public_key_response.rs` | `2cf83b94aa23f7da7ce730e64ded7be3e6337405c65ca2ed5b72a68a1be473c2` | §§3 and 6 predecessor/current secret selection |

The hash wrapper calls `operation_result` in
`crates/tith-crypto/src/signature.rs`, SHA‑256
`6beb48689f71677ae483ea4d1a3682ae80f7ebc7a09bf343e21901bc70cab1ee`.
That helper and the cryptographic SignedTLV behavior are covered by the
concurrently refreshed TTS‑0003 record and run
[`32642066029`](https://github.com/RealDeuce/tith/actions/runs/32642066029).
The other transitive protocol helpers are the exact qualified TTS‑0002,
TTS‑0003, TTS‑0004, TTS‑0008, and TTS‑0009 dependency surfaces named above.
Vendored Libhydrogen is external and outside the TITH-owned surface.

## Test manifest

| Test file and mapped tests | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-crypto/src/hash.rs`: both hash tests | `f4759818ced5a6a117f69b48672606aab6492adeb4768f1939dcbc3a6d539145` | §4 exact context, bytes, streaming, and error translation |
| `crates/tith-wire/src/bundle.rs`: all 13 Bundle tests | `34bd9b2798288641945595cbc3f1310c68164baad9e939c0949218f891609b04` | §§2, 5, and 6 complete Bundle and probe grammar |
| `crates/tith-wire/src/bundle_types.rs`: assignment/request test | `701ce8edf6219c9b93c37bbbd95136b39441df656e902c7e0196ed9d5535deaf` | §§2–4 assignments and request membership |
| `crates/tith-wire/src/item_carrier_tests.rs`: all three carrier tests | `3342647883741c008678e1dcf65a7737627776fe3875a42e4ba5ad44b846df40` | §§3–6 every request, response, reason, and identifier boundary |
| `crates/tith-exchange/src/lib.rs`: response/session tests | `02f928e6537aebf32370c5e187d3fd44d2f916291b4f8ff6fb594e16f4c17b4d` | §§4 and 6 correlation, ordering, completion, and active close |
| `crates/tith-exchange/src/response.rs`: request-kind test | `71e8afe69af37b1f0212320fac9ca1c516f31ff8c9eecf563eff6fd32e78f668` | §§3 and 6 complete request accounting classes |
| `crates/tith-store/src/lib.rs`: signed acceptance and invalid-carrier tests | `a9d406f880d98ad89e5f4b51c1d57186c8f865a1e3248c00e053e5e3eabc3f18` | §7 durable idempotent acceptance and explicit removal |
| `crates/tith-store/src/outbound.rs`: four Poll snapshot tests | `b89dc7bf7bd835a26f9db1c0442a4ea1fd69f50ae6ddda28b595eafa3e270eed` | §3 atomic complete, identity-scoped, kind-correct snapshots |
| `crates/tithd/src/mail.rs`: live probe, acceptance, Poll, and final-reply tests named above | `4f59b9358c40a6d41a5d1e1dd51d5497be88d9e49785c9959084e32fb612b871` | §§3, 5–7 live interoperability and persistence witnesses |
| `crates/tith-wire/src/item_format_tests.rs`: complete item carrier inputs | `0b8024c882c9ecaa2da36c4800efea2864aeae2460e66935eb5f6bb735874a2c` | §§3 and 7 qualified TTS‑0009 item and identity witnesses |

## Targets, toolchains, and commands

Pinned repository inputs:

| Input | SHA‑256 or exact version |
| --- | --- |
| `Cargo.lock` | `c28e54f12c2b5a8150bfdf2a7555ed13e690b9f7de55b00452fafb8e73c30656` |
| `rust-toolchain.toml` | `da4fa49758e2d8da0f35f79049581134c98b02db0176e515e62367bf9242047c` |
| `.github/workflows/rust.yml` | `05ea7430f172323d474b5dfea8bfb57598a8fbe955d725ff681fa93f58b79ac6` |
| `.github/workflows/qualify-tts-0005.yml` | `760d4eaea154cfc69cdb6c41a729743e77c4077db71a14e9f9bb956c6c7d5c1e` |
| `qualification/check_source_line_coverage.py` | `a54efa84ebe2e69a1916433e5cf166283b5316cfa862a73a443e0b2bd692d957` |
| GitHub `actions/checkout` | `3d3c42e5aac5ba805825da76410c181273ba90b1` |
| GitHub `dtolnay/rust-toolchain` | `6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772` |

Normal validation used Rust 1.97.1, compiler commit
`8bab26f4f68e0e26f0bb7960be334d5b520ea452`, with rustfmt and Clippy on
FreeBSD 14.4‑RELEASE‑p3, GitHub Ubuntu 24.04 image `20260816.277.1`, and
GitHub Windows Server 2025 image `20260818.207.1`.

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

GitHub also ran `cargo doc --workspace --no-deps`. Coverage used Ubuntu image
`20260816.277.1`, `nightly-2026-08-20` (`rustc 1.100.0-nightly`,
commit `f7d782a3b`, 2026‑08‑19), `llvm-tools-preview`, and
`cargo-llvm-cov 0.9.0 --locked`.

```sh
cargo test --workspace
cargo +nightly-2026-08-20 llvm-cov clean --workspace
cargo +nightly-2026-08-20 llvm-cov --branch --workspace --json \
  --output-path tts-0005-coverage.json
cargo +nightly-2026-08-20 llvm-cov report --branch --text \
  --show-missing-lines
python3 qualification/check_source_line_coverage.py \
  tts-0005-coverage.json \
  crates/tith-crypto/src/hash.rs \
  crates/tith-wire/src/bundle.rs \
  crates/tith-wire/src/bundle_types.rs \
  crates/tith-wire/src/item_carrier.rs \
  crates/tith-exchange/src/response.rs \
  crates/tith-store/src/duplicate_identity.rs \
  crates/tith-store/src/inbound_accept.rs \
  crates/tith-store/src/poll_snapshot.rs \
  crates/tithd/src/public_key_response.rs
```

## Results and coverage

- FreeBSD: formatting, strict Clippy, all 456 workspace tests, and doc tests
  passed with the exact implementation and tests.
- Ubuntu and Windows: GitHub Rust run
  [`32650822745`](https://github.com/RealDeuce/tith/actions/runs/32650822745)
  passed formatting, strict Clippy, all workspace tests, and documentation at
  the exact implementation/test commit immediately preceding the
  evidence-trigger correction. Ubuntu ran 456 tests and Windows ran 455
  because one daemon test is Unix-only.
- Coverage: GitHub run
  [`32653032050`](https://github.com/RealDeuce/tith/actions/runs/32653032050)
  passed at the evidence commit. It ran the complete 456-test workspace before
  producing and checking the report.

| Surface | Functions | Lines | Branches |
| --- | ---: | ---: | ---: |
| `hash.rs` | 13/13 | 71/71 | 0/0 |
| `bundle.rs` | 47/47 | 924/924 | 106/106 |
| `bundle_types.rs` | 3/3 | 29/29 | 0/0 |
| `item_carrier.rs` | 17/17 | 259/259 | 32/32 |
| `response.rs` | 25/25 | 214/214 | 24/24 |
| `duplicate_identity.rs` | 2/2 | 24/24 | 0/0 |
| `inbound_accept.rs` | 1/1 | 72/73 aggregate; no uncovered primary source line | 16/16 |
| `poll_snapshot.rs` | 2/2 | 74/74 | 8/8 |
| `public_key_response.rs` | 2/2 | 32/32 | 2/2 |

LLVM's aggregate line counter reports one mapped include-boundary line in
`inbound_accept.rs` while its annotated report and JSON name no uncovered
primary source line. The pinned checker fails for any real uncovered source
line and separately requires every function and measured branch. This is
complete source-line coverage under TPS‑0001, not an exclusion.

The coverage JSON SHA‑256 is
`329dcb0804c2a5b5430008964fe6ee2e8367b02b7afbd83846f9ab3b7cb4c45e`.

## Issue audit

GitHub searches for ASCII and typographic TTS‑0005 names, “Bundle Format,”
PublicKeyRequest, TLVHash, Poll snapshot, Reply Bundle, and duplicate
acceptance reviewed issues #19, #49, #57, #64, #65, #80, #105–#114.
Every technical standards defect found in this qualification is closed by its
correcting commit. Issues #111–#114 are likewise closed by their editorial,
evidence-maintenance, and qualification commits.

Issue #19 concerns later application handling of an already parsed
FileRequest, not this wire-format conformance surface. No open issue alleges a
normative defect, ambiguity, missing requirement, security defect,
wire incompatibility, or external blocker in TTS‑0005 or a normative
dependency.

## Security review

Unauthenticated bytes first cross canonical TTS‑0002/TTS‑0003 framing. The
Bundle parser requires the literal Origin, adjacent conditional keys, exact
Header grammar, and exact Header hash before payload contents are classified.
Malformed framing stops at the failing offset; malformed or unauthenticated
SignedTLV data is never exposed as accepted work. Unknown values cannot satisfy
assigned requirements or split an adjacent address/key pair.

A non-anonymous key supplied on the wire cannot override TTS‑0008 resolution.
The sole exception is the dedicated PublicKeyRequest reply, whose complete
shape, reversed identities, request identifier, payload hash, advertised key,
Accepted successor, and signature are checked together. A predecessor proof is
accepted only under the exact requested key. A self-signed first-contact result
is merely an observation until the exact contact is locally authorized.
Failure under the requested predecessor does not fall back to the current key
or create trust evidence.

RequestIdentifier alone is not authority. Every response is bound to the exact
encoded request SignedTLV by TLVHash, and completion records the pair. Equal
identifiers in different payloads or directions remain distinct. Duplicate,
unexpected, unauthenticated, and incomplete responses fail; the Client
completes from authenticated outstanding-response accounting and does not wait
for Server FIN.

Poll selection occurs in one redb write transaction. It claims every matching
unclaimed copy or none if the set is empty, scopes anonymous work by exact key,
and leaves active or later work outside the snapshot. Errors while building a
reply release every claimed copy, as exercised by the daemon fault path.

Acceptance validates carrier kind, authentication state, and stable signed
identity before one transaction writes the item, record, and duplicate index.
A duplicate lookup returns the earlier durable identifier and does not create
another item. Records have no automatic expiry; explicit removal is separately
tested. Store failure is not converted to Accepted, so responsibility remains
with the sender.

Fixed-size hashes and keys reject other lengths. Hashing and signature native
failures propagate as local errors. Persistent operations use checked
transactions; malformed carrier combinations, wrong request correlation,
wrong signer, and interrupted exchange states fail closed. The implementation
introduces no arbitrary Bundle or payload size limit.

## Content-manifest verification

Qualifying TTS‑0006 isolated the shared response accounting from the session
module and expanded the mapped live daemon witnesses. The resulting
`response.rs`, `lib.rs`, and `mail.rs` digests are recorded above. The new
response implementation retains every TTS‑0005 request class, exact
hash/identifier correlation, arbitrary response order, and completion rule;
the replacement coverage run exercises its complete 25-function,
214-line, 24-branch surface.

The qualified document, dependencies, every other conformance source and
mapped test, lockfile, toolchain, normal Rust workflow, checker, and external
Libhydrogen document remain byte-for-byte identical. The qualification
workflow changed only by adding its four previously omitted mapped-test path
triggers; its new digest is recorded above. Pinned qualification run
[`32653032050`](https://github.com/RealDeuce/tith/actions/runs/32653032050)
passed at the exact replacement evidence commit. Normal Rust run
[`32650822745`](https://github.com/RealDeuce/tith/actions/runs/32650822745)
passed the byte-for-byte identical implementation and tests before the
qualification-workflow trigger correction. No TTS‑0005 normative or
conformance result changed.
