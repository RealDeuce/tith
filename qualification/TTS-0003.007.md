# Qualification: TTS‑0003 revision 7

- Candidate: existing TTS‑0003 revision 7 at `ef3c5f375ce6b5337531b4a4bbeb857c5d0cc971`
- Publication date: 2026‑08‑22
- Qualified document SHA‑256: `1e1e8d25a5d819568c1f18f70b161ad0c816196f9e6b62d18181fffd144200be`
- Evidence commit: `c335964aef6909bced83d9992fc85329c8575378`
- TPS revision: TPS‑0001 revision 2, SHA‑256 `3e52cba974d025fafc7a0b0dfbe763824c238e14dd7ec0a04f865ec4409c59f6`
- Approval: Stephen Hurd, 2026‑08‑22

TTS‑0003 revision 7 is an existing standard undergoing its first
qualification under TPS‑0001. It is not a TSP promotion, and its identifier
and revision do not change when this record is published.

## Normative dependencies

| Publication | Revision | SHA‑256 or exact external version |
| --- | ---: | --- |
| TTS‑0002 | 3 | `b073ecb328a67a2477db49e7a3f8d1e3da5c5c05caf0709d398fc1006a906dbb` |
| TTS‑0004 | 5 | `abede4c5e0d559ff0dd57967f5e6aefc9aabd380d20ec17924e0fcb63e25a7b6` |
| FTA‑1006 | 2 | `2265e8410311850f4be3068aed446876e017d45d70dd3d41bb3c3991da6f20e4` for `fta-1006.002` |
| Libhydrogen, “Public-key signatures” | Version 1 | Git commit `705149f9c8db1f400775477adb6f9c2254103dca`, file `Public-key-signatures.md`, SHA‑256 `15700b0cb5225b26eb6c97fcaa184c0e3f31e24404a9221e3bedc8edb88f13ee` |
| POSIX Seconds Since the Epoch | Issue 8, XBD §4.19 | The Open Group Base Specifications Issue 8, 2024 edition, at the URLs pinned in TTS‑0003 |

TTS‑0002 and TTS‑0004 have no internal normative dependencies. The
resulting graph is `TTS‑0003 → {TTS‑0002, TTS‑0004}` and is acyclic.

## Conformance roles and features

| Role | Mandatory features | Optional features |
| --- | --- | --- |
| Type-registry author | Reserve 0–31 and use assignments 0–9 exactly | None |
| TLV producer | Never emit Type 0 | None |
| TLV consumer | Treat Type 0 as terminal malformation of its sequence | None |
| Common-value producer and consumer | Origin, Destination, Address, PublicKey, SignedOrigin, and non-negative Timestamp representations | None |
| Signer | Exact `SignTLV` context, SignedData bytes, and 64-byte Signature | None |
| Verifier | Applicable-key selection, exact-byte verification, and no action on unauthenticated SignedData | Limited structural parsing is implementation discretion, not an optional protocol feature; the reference implementation provides it |
| SignedTLV producer and consumer | Complete child grammar, local or inherited Origin, unknown-child policy, and malformed/invalid separation | None |
| Enclosing-standard author | Define common-value semantics, key source, allowed extensions, malformed disposition, and authentication consequences where delegated | None |

Every executable syntax and operation owned by TTS‑0003 is implemented. Its
`MAY` for limited unauthenticated parsing permits discretion rather than naming
an optional protocol capability; the implementation nevertheless supplies and
tests that path.

## Requirement matrix

`S` is `signature.rs`, `C` is `common.rs`, `E` is `error.rs`, `Z` is
`type_code.rs`, and `K`, `A`, `I`, and `U` are the qualified PublicKey,
Address, Identity, and unsigned-integer dependency surfaces. Complete results
are recorded below.

| Requirement | Role | Implementation | Positive evidence | Negative evidence | Coverage |
| --- | --- | --- | --- | --- | --- |
| §2 reserves 0–31 and prohibits redefinition | Registry author | Common constants and the common arms of `is_defined` in `types.rs` | `common_assignments_are_exact_and_the_rest_of_the_range_is_reserved` checks 1–9 | Same test verifies 0 is defined and 10–31 remain unassigned | Complete direct test; unrelated assignments share the file |
| §3 Type 0 producer prohibition | TLV producer | `require_defined_type`, `TlvHeader::new`, `OwnedTlv::new` | `producers_accept_every_nonzero_type_and_reject_zero` accepts 1 and `u64::MAX` | Same test and `type_zero_is_never_produced_and_terminates_its_sequence` reject 0 through owned and streaming producers | Z; TLV integration witness |
| §3 Type 0 terminates the containing sequence with no resumption | TLV consumer | `SequenceTypeState`, `parse_sequence`, `TlvReader::read_next` | Valid values before the invalid type are consumed | `a_type_zero_permanently_terminates_the_sequence_state` and `type_zero_is_never_produced_and_terminates_its_sequence` prove no later value is returned | Z; TLV integration witness |
| §3 enclosing standard owns a Type 0 consequence | Enclosing-standard author | Normative-document audit | Current enclosing standards define their malformed-object or transport result | Not applicable: this constrains specifications, not a runtime input | Not executable |
| §3 Origin uses a TTS‑0004 address and enclosing semantics | Common-value producer/consumer and standards author | `Address`, `address_value`, enclosing parsers | Common and bundle round trips cover anonymous and non-anonymous Origin | `malformed_common_values_fail_at_their_exact_boundary` covers UTF‑8 and address failures | A, C |
| §3 anonymous Origin carries its exact adjacent PublicKey | Producer/consumer | `build_signed_tlv`, `signed_tlv_parts`, `identity` | `signed_tlv_carries_an_anonymous_origin_key_and_unknown_extension` | `anonymous_origin_and_key_must_be_adjacent`, short-key cases | K, A, I, C |
| §3 non-anonymous Origin uses only its enclosing resolver and supplied key cannot override it | Verifier | `KeyResolver`, `identity`, item provenance verification | `non_anonymous_origin_uses_only_the_resolver_key` | Same test rejects a supplied key; `origin_key_prevents_signed_origin_fallback` proves no fallback after failure | K, A, I, C; item integration witness |
| §3 Signature is 64 bytes and uses the exact eight-byte `SignTLV` context | Signer/verifier | `Signature`, `TlvSigner`, `sign_tlv`, `verify_tlv` | `deterministic_signing_and_streaming_verification_cover_the_exact_context` compares one-shot and streaming results | The same test rejects a changed context/data/signature/key; common tests reject wrong length | S, C |
| §3 a containing type defines signed bytes, applicable identity, and failure consequence | Enclosing-standard author | Normative-document audit and typed enclosing parsers | Bundle and item tests authenticate their exact documented regions | Not applicable: missing definitions are document defects, not one runtime form | Not executable; integration witnesses |
| §3 SignedData has a same-level Signature and the most recent applicable Origin | SignedTLV consumer | `signed_tlv_parts`, `verify_signed_tlv_with_policy` | Local and inherited identities both verify | `signed_container_rejects_each_invalid_defined_ordering` and `signed_container_requires_an_origin_source_and_exact_signature_length` | C, E |
| §3 SignedTLV has exactly one SignedData followed by one Signature and optional first Origin | Producer/consumer | `build_signed_tlv`, `signed_tlv_parts` | Anonymous, non-anonymous, and inherited round trips | Ordering test covers reversal, duplication, omission, and prohibited defined children | C, E |
| §3 SignedTLV PublicKey cardinality and placement | Producer/consumer | `build_signed_tlv`, `signed_tlv_parts` | Anonymous producer emits exactly one adjacent key | Anonymous adjacency and non-anonymous supplied-key tests reject prohibited forms | K, A, I, C |
| §3 unknown SignedTLV children follow enclosing policy without changing defined order/cardinality | Consumer and standards author | `UnknownChildren`, `verify_signed_tlv_with_policy` | Anonymous extension test accepts under Allow | Same bytes are rejected under Reject; defined children are always rejected out of grammar | C |
| §3 SignedData Value is canonical TLV and Signature covers its exact Value bytes | Signer/verifier | `concatenate`, `build_signed_tlv`, `verify_signed_tlv` | Signed child round trips preserve Type, Length, and Value bytes | Altered signature/data and malformed nested TLV tests fail before authenticated data is returned | S, C, E, U |
| §3 malformed SignedTLV disposition is enclosing policy | Enclosing-standard author | Typed enclosing parsers and error translation | Each enclosing parser maps valid SignedTLV values | Malformed common-value tests preserve the exact framing/grammar category | C, E; enclosing integration witnesses |
| §3 invalid Signature prohibits processing or action | Verifier | `verify_signed_tlv` returns data only after verification | Valid signature returns `VerifiedSignedTlv` | Bad signatures return `InvalidSignature`; bundle/item acceptance tests refuse unauthenticated action | S, C, E; integration witnesses |
| §3 limited unauthenticated parsing only exposes structurally valid SignedData | Verifier | `unauthenticated_signed_data` | `unauthenticated_access_checks_structure_but_not_signature` exercises the permitted path | Non-SignedTLV and malformed nested data are rejected | C, E |
| §3 Timestamp is a non-negative TTS‑0002 integer representing POSIX Seconds Since the Epoch | Common-value producer/consumer | `encode_u64`, `decode_u64`, typed timestamp fields | TTS‑0002 boundaries and bundle/item timestamp round trips | TTS‑0002 rejects malformed/overflowing values; unsigned type cannot represent a pre-Epoch instant | U; bundle/item integration witnesses |
| §3 Destination and Address contain TTS‑0004 text | Common-value producer/consumer | `Address` formatting and parsing in enclosing modules | Complete canonical TTS‑0004 round trips and typed bundle/item round trips | Complete address grammar rejection plus malformed UTF‑8 witnesses | A; integration witnesses |
| §3 PublicKey is exactly the 32 raw bytes defined by TTS‑0004 and assigns no address | Common-value producer/consumer | `PublicKey`, `public_key_value`, private trust store boundary | Valid anonymous identities carry exact key bytes | 31-byte value is rejected; parsing alone never writes a pin | K, I, C; trust integration witnesses |
| §3 SignedOrigin placement and coverage are enclosing-defined; it is only fallback and never replaces Origin | Common-value consumer and standards author | Item provenance parser and verifier | `signed_origin_authenticates_when_origin_has_no_key`, `standalone_file_uses_signed_origin_fallback` | `origin_key_prevents_signed_origin_fallback`, anonymous adjacency tests | A, K, I; item integration witnesses |

## Conformance-surface manifest

The common algorithms were isolated so unrelated bundle and item grammar does
not enter this standard's surface. Qualified dependency modules are included
because their exact behavior is needed by the common values.

| Source item | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-crypto/src/public_key.rs` | `b5987f4a8d7863d45bcfda6dadcfc54ce19f50059bfd8a1291f2bde0165be1bf` | Fixed-size PublicKey dependency |
| `crates/tith-crypto/src/signature.rs` | `3684e84a0f2f97f94702b8a5e5e69c692bcaf444b5eb12ab21ddc5371d9a7310` | Signature values, secret handling, `SignTLV` signer and verifier |
| `crates/tith-wire/src/address.rs` | `6ec227cf3c39f3e4b601a0c6f580a0536f89c4f3684bfc984e8a93a0280cbce6` | Origin, Destination, Address, and SignedOrigin dependency |
| `crates/tith-wire/src/common.rs` | `2d6edb40801ac44e977019c05306a7293a18c6f71c8489337b45188002a0f516` | Common values, key resolver, SignedTLV production and verification |
| `crates/tith-wire/src/error.rs` | `c589d1b547cfaf90c6cc4531aea72c192726bf83a600ebad9bb99cd804523304` | Public common-value and authentication failure results |
| `crates/tith-wire/src/identity.rs` | `b7f8fdcc1725b2817d47139a7fde453818fd66378b5f5eefa471059a77bff7f1` | Address/public-key identity pairing |
| `crates/tith-wire/src/integer.rs` | `4f040dd75ed464be560f629d8afe984c4403fdc20a279315d2859b51e696f7f3` | Timestamp and nested TLV unsigned-integer dependency |
| `crates/tith-wire/src/type_code.rs` | `db1bbe2967e6d758fbbe2a6ad76c12377c4836c52930941a8d726f1a4db44638` | Type 0 producer prohibition and terminal sequence state |

`types.rs` supplies the exact common constants and `is_defined` classification;
its complete-file digest is
`b67fe15009ca1d853ca9b759fc5044888415fe252cbeb7bd9704625f926ee120`.
The same file also contains unrelated TTS‑0005/TSP‑0016 assignments and
request classification, which are outside this surface. `tlv.rs` delegates
the Type 0 decision to covered `type_code.rs`; its generic framing and I/O are
an integration vehicle rather than another TTS‑0003 decision. Enclosing
bundle/item modules own their standards' placement and consequences and are
integration witnesses, not hidden common-type alternatives.

## Test manifest

Tests embedded in a source file use the SHA‑256 digest of that complete file.

| Test file and mapped tests | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-crypto/src/signature.rs`: all three `signature::tests` | `3684e84a0f2f97f94702b8a5e5e69c692bcaf444b5eb12ab21ddc5371d9a7310` | Exact context, bytes, lengths, keys, streaming, error paths, and secret redaction |
| `crates/tith-wire/src/common.rs`: all seven `common::tests` | `2d6edb40801ac44e977019c05306a7293a18c6f71c8489337b45188002a0f516` | Complete common-value and SignedTLV grammar/authentication behavior |
| `crates/tith-wire/src/error.rs`: both `error::tests` | `c589d1b547cfaf90c6cc4531aea72c192726bf83a600ebad9bb99cd804523304` | Every public result and translation |
| `crates/tith-wire/src/type_code.rs`: both `type_code::tests` | `db1bbe2967e6d758fbbe2a6ad76c12377c4836c52930941a8d726f1a4db44638` | Producer Type 0 rejection and permanent consumer termination |
| `crates/tith-wire/src/types.rs`: common-assignment test | `b67fe15009ca1d853ca9b759fc5044888415fe252cbeb7bd9704625f926ee120` | Exact 0‑9 assignments and reserved 10‑31 range |
| `crates/tith-wire/src/tlv.rs`: Type 0, canonical codec, malformed length, truncation, I/O, and allocation tests | `32b806c4e8ba92765468eb4f36b9800a2ca92712573df3fff8996954f76d7cc5` | TLV integration and untrusted framing faults |
| `crates/tith-wire/src/bundle.rs`: signed-container, key, timestamp, and exact-hash tests | `aedbd5133e401713fc8370b7e68affa95b76046f1bbb4c8522ce6876d674c264` | Enclosing Bundle integration |
| `crates/tith-wire/src/item.rs`: SignedOrigin fallback/override, anonymous key, malformed signature, timestamp, and round-trip tests | `0f051e0ca2facbf73667e5d830fc9ac088efd0592be80ef2d8d64604bcedee95` | Enclosing item integration |

The complete tests in the qualified dependency modules are recorded in their
TTS‑0002 and TTS‑0004 qualification records and ran again here.

## Targets, toolchains, and commands

Pinned repository inputs:

| Input | SHA‑256 or exact version |
| --- | --- |
| `Cargo.lock` | `c28e54f12c2b5a8150bfdf2a7555ed13e690b9f7de55b00452fafb8e73c30656` |
| `rust-toolchain.toml` | `da4fa49758e2d8da0f35f79049581134c98b02db0176e515e62367bf9242047c` |
| `.github/workflows/rust.yml` | `05ea7430f172323d474b5dfea8bfb57598a8fbe955d725ff681fa93f58b79ac6` |
| `.github/workflows/qualify-tts-0003.yml` | `6b93015bec7ae6c823d07beecb0f24a545e1d533005bee570746d0e51616bfdd` |
| `qualification/check_source_line_coverage.py` | `3349851d293b99c60aacb208781f468397456c926e0aae52e484ed3bc27b77b8` |
| GitHub `actions/checkout` | `3d3c42e5aac5ba805825da76410c181273ba90b1` |
| GitHub `dtolnay/rust-toolchain` | `6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772` |

Normal validation used Rust 1.97.1, compiler commit
`8bab26f4f68e0e26f0bb7960be334d5b520ea452`, with rustfmt and Clippy, on:

- `x86_64-unknown-freebsd`, FreeBSD 14.4‑RELEASE‑p3;
- `x86_64-unknown-linux-gnu`, GitHub Ubuntu 24.04 image
  `20260816.277.1`; and
- `x86_64-pc-windows-msvc`, GitHub Windows Server 2025/Visual Studio 2026
  image `20260818.207.1`.

Commands on every normal target:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

The GitHub targets additionally ran `cargo doc --workspace --no-deps`.

Coverage used Ubuntu image `20260816.277.1`,
`nightly-2026-08-20` (`rustc 1.100.0-nightly`, commit `f7d782a3b`,
2026‑08‑19), `llvm-tools-preview`, and `cargo-llvm-cov 0.9.0 --locked`.
The exact coverage commands were:

```sh
cargo test --workspace
cargo +nightly-2026-08-20 llvm-cov clean --workspace
cargo +nightly-2026-08-20 llvm-cov --branch \
  --package tith-crypto --package tith-wire --lib \
  --json --output-path tts-0003-coverage.json
cargo +nightly-2026-08-20 llvm-cov report --branch --text \
  --show-missing-lines
python3 qualification/check_source_line_coverage.py \
  tts-0003-coverage.json \
  crates/tith-crypto/src/public_key.rs \
  crates/tith-crypto/src/signature.rs \
  crates/tith-wire/src/address.rs \
  crates/tith-wire/src/common.rs \
  crates/tith-wire/src/error.rs \
  crates/tith-wire/src/identity.rs \
  crates/tith-wire/src/integer.rs \
  crates/tith-wire/src/type_code.rs
```

## Results and coverage

- FreeBSD: formatting and Clippy passed; all 404 workspace tests and all doc
  tests passed.
- Ubuntu and Windows: GitHub Rust run
  [`32611196874`](https://github.com/RealDeuce/tith/actions/runs/32611196874)
  passed formatting, Clippy, workspace tests, and documentation on both
  targets at the evidence commit. Ubuntu ran 404 tests; Windows ran 403
  because one daemon test is Unix-only.
- Coverage: GitHub run
  [`32611196873`](https://github.com/RealDeuce/tith/actions/runs/32611196873)
  passed at the evidence commit. It first ran all 404 workspace tests, then
  ran all 5 `tith-crypto` and 79 `tith-wire` tests while instrumenting the
  isolated surface.

The final coverage run established:

| Surface | Functions | Lines | Branches |
| --- | ---: | ---: | ---: |
| `public_key.rs` | 3/3 | 9/9 | 0/0 |
| `signature.rs` | 23/23 | 153/153 | 2/2 |
| `address.rs` | 64/64 | 759/759 | 166/166 |
| `common.rs` | 25/25 | 303/303 | 26/26 |
| `error.rs` | 7/7 | 56/56 | 0/0 |
| `identity.rs` | 3/3 | 21/21 | 4/4 |
| `integer.rs` | 9/9 | 107/107 | 12/12 |
| `type_code.rs` | 6/6 | 35/35 | 6/6 |

Its JSON report SHA‑256 was
`40fd4b3f808cffd7fed8ef0be2501fa9d6d9fae941575752997f79cab12726a4`.
The report itself need not remain available: the digest, complete summaries,
pinned inputs, and reproduction commands are durable here.

The signer output is consumed through the independently exercised verifier,
both one-shot and streaming paths use the same exact wire context, and common
values round-trip through separately selected producer and consumer entry
points. No platform-conditional TTS‑0003 surface exists.

## Issue audit

GitHub searches for ASCII and typographic TTS‑0003 names, the title, and the
defined common types found closed standards issues #4, #62, #63, and #98–#100.
Issue #65 concerns TTS‑0005's dependency on the former TSP‑0003 and does not
allege a TTS‑0003 defect. Issue #101, which this record's publishing commit
will close, is the only open issue about the candidate. The qualified
TTS‑0002 and TTS‑0004 dependency audits found no unresolved normative defect,
ambiguity, security defect, incompatibility, or external blocker.

## Security review

Type, Length, and common values can arrive before authentication. Type 0 is
rejected before its Length or Value is interpreted, permanently poisons the
streaming sequence state, and cannot be used as a resynchronization marker.
The owned parser returns no partial result after an invalid type. Canonical
integer decoding, checked platform conversion, exact-length reads, truncation,
allocation overflow, interrupted reads, and hard I/O failures are exercised at
the generic TLV boundary.

Signing uses the exact eight-byte `SignTLV` context including its terminal NUL.
Public keys and signatures are fixed-size types; every other length is rejected.
The Signature covers the literal SignedData Value bytes before nested values
are exposed as authenticated. The secret key is redacted from `Debug` and
cleared through `hydro_memzero` on drop. Every native initialization and
operation result is translated to a typed failure.

An anonymous Origin must carry its exact adjacent key. A non-anonymous Origin
accepts no supplied override and obtains one applicable key from the enclosing
resolver. An available Origin key always wins over SignedOrigin, and failed
verification is not retried under another identity. Thus attacker-controlled
key material cannot replace an authoritative key or turn one failed signature
into an alternate authentication attempt.

`verify_signed_tlv` returns SignedData only after authentication. The separate
unauthenticated accessor performs structural validation and exists solely for
the enclosing protocol's bounded error-response path; its result type carries
no authenticated identity. Enclosing acceptance tests demonstrate that data
from an invalid signature is neither processed nor acted upon. Unknown children
are an explicit enclosing-policy choice and never relax the order or
cardinality of defined children.

The common layer has no persistence, concurrent ownership, or recovery state.
Applicable malformed, overflow, truncation, resource, and I/O paths are covered
by the negative and fault-injection evidence above.

## Content-manifest verification

This is the first qualification of TTS‑0003. TTS‑0002 revision 3 and TTS‑0004
revision 5 remain byte-for-byte unchanged. Their records are replaced in the
same publishing commit because the shared TLV and signed-container integration
witnesses changed during this qualification; each applicable workflow was
rerun against its exact changed manifest.

The TTS‑0003 normal and coverage runs used the exact revision-7 document,
dependencies, conformance sources, mapped tests, lockfile, toolchain,
workflows, and checker bytes listed above. Publishing this record and the two
replacement dependency records changes no qualified input.
