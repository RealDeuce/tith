# Qualification: TTS‑0009 revision 2

- Candidate: existing TTS‑0009 revision 2 at `7cdf7c4fc6b78599d5a3edd71bb1400844fb8dc0`
- Publication date: 2026‑08‑23
- Qualified document SHA‑256: `bab865d88e53da1c40f3c27468d5e5cfcc825ae94998b6ef42fb0c29f007ac36`
- Evidence commit: `28e34d4e0e6b2dacd78520bdaa79f4f7700978a9`
- Carried record: `qualification/TTS-0009.001.md`, SHA‑256 `8454d30f23b100aabbdbcfc6d6b6468d0955e11550ca047ffce0cc9b9311c6f7`
- TPS revision: TPS‑0001 revision 2, SHA‑256 `3e52cba974d025fafc7a0b0dfbe763824c238e14dd7ec0a04f865ec4409c59f6`
- Approval: Stephen Hurd, 2026‑08‑23

TTS‑0009 revision 2 is an editorial revision of the already qualified
revision 1. The repository owner determined that the complete difference below
changes no conforming behavior, wire or file data, assigned value, dependency
ownership, or conformance-test result. The corrected TTS‑0004 citation now
identifies qualified revision 5, and this record corrects the revision labels
for the unchanged TTS‑0002 revision‑3 and TTS‑0007 revision‑2 digests.

## Complete textual difference

These are all changes from TTS‑0009 revision 1 to revision 2:

- `Revision: 1` became `Revision: 2`.
- Appendix A added the exact FTA‑1006.002 requirement-keyword reference.
- The TTS‑0004 citation date changed from 2026‑08‑21 to 2026‑08‑22.
- The history gained the revision‑2 entry stating that unchanged requirements
  were carried forward with corrected citations and revision labels.

No other byte in the document changed.

## Normative dependencies

| Publication | Revision | SHA‑256 or exact external version |
| --- | ---: | --- |
| TTS‑0002 | 3 | `b073ecb328a67a2477db49e7a3f8d1e3da5c5c05caf0709d398fc1006a906dbb` |
| TTS‑0003 | 7 | `1e1e8d25a5d819568c1f18f70b161ad0c816196f9e6b62d18181fffd144200be` |
| TTS‑0004 | 5 | `abede4c5e0d559ff0dd57967f5e6aefc9aabd380d20ec17924e0fcb63e25a7b6` |
| TTS‑0007 | 2 | `1abded25472f714860772098a6471b1cb4c51f3a849263f2d17523ec9e9afcaf` |
| FTA‑1006 | 2 | `2265e8410311850f4be3068aed446876e017d45d70dd3d41bb3c3991da6f20e4` for `fta-1006.002` |
| FTS‑0001 | 16 | `4c6456943b8631d2ab8107e4964c2505c2b9c1380191aaced9b1da8f21f96b64` for `fts-0001.016` |
| FTS‑0004 | 1 | `bef3166c8d5f4ee11e3facb8cb07c2916dae715ab6315f631f3167ea53906be1` for `fts-0004.001` |
| FSC‑0068 | 1 | `d7c0b39291aeaf4f151a84fa576714eaaa7f26c1aeebb968e725102bf901d393` for `fsc-0068.001` |

The resulting internal graph has only edges from TTS‑0009 to TTS‑0002,
TTS‑0003, TTS‑0004, and TTS‑0007 and is acyclic.

## Conformance roles and features

| Role | Mandatory features | Optional or conditional features |
| --- | --- | --- |
| Item producer | Canonical Message, embedded and standalone File, and FileRequest child sequences; canonical child values | NetMail/EchoMail, attached/peer/distribution File, optional metadata, optional Signature, SignedOrigin, and unknown extensions are all implemented |
| Item consumer | Exact grammar, UTF‑8, integer, address, key, ordering, cardinality, and prohibited-shape validation | Every optional value and authentication outcome is consumed |
| Authenticator | Exact signed-region verification, one effective signer, no fallback, five Message/File outcomes, Transport FileRequest outcome, stable identity only after success | Origin and SignedOrigin, anonymous and non-anonymous signers, valid, invalid, and unsigned paths are implemented |
| Forwarder/reserializer | Preserve signed bytes and Signature; mutate only the suffix; append canonical Via/SeenBy without changing identity | Message and distribution-File forwarding are implemented |
| Conversion boundary | Preserve an existing native authentication state exactly or refuse it; mint ordinary legacy input as a new item | Exact retained-signature reconstruction and refusal are implemented by the adapter boundary |
| Replaces consumer | Case-sensitive whole-Filename Unicode-scalar glob | Candidate selection and replacement action are deliberately local policy, not optional protocol features |

## Requirement matrix

`I` is complete coverage of `item_format_impl.rs`; `D` is complete coverage
of `item_format.rs`, as recorded below.

| Requirement | Role | Implementation | Positive evidence | Negative evidence | Coverage |
| --- | --- | --- | --- | --- | --- |
| §2 all sequences use canonical TTS‑0002 TLVs and TTS‑0003/TTS‑0004 values | Producer/consumer | `OwnedTlv`, `Cursor`, typed address/integer/key helpers | Message, File, request, Via, ReplyTo, and model round trips | malformed TLV, integer, address, key, and nested-length suites | I plus qualified dependencies |
| §2 literal first child, adjacent conditional key, known ordering/cardinality, and unknown-extension placement | Producer/consumer | `Cursor`, `conditional_public_key`, model byte retention | origin-first and unknown-extension round trips | unknown-before-Origin, separated key, misplaced assigned type, duplicate, and missing-child tests | I |
| §2 signed-region and mutable-suffix unknown values retain exact positions | Producer/reserializer | `encoded_prefix`, `ItemModel`, retained/forwarded builders | extended signed Message survives forwarding byte-exactly | authentication and grammar tests reject an extension used as a required child | I |
| §3.1 Message is exactly NetMail or EchoMail with the complete ordered child grammar | Producer/consumer | Message builders, validator, reader, and `MessageModel` | originated/retained/read round trips for both forms | neither/both Destination/Area, prohibited echo controls, missing Via, malformed suffix, and wrong-model tests | I |
| §3.2 embedded, peer-addressed, and distribution Files use their exact distinct grammars | Producer/consumer | File builders, validator, reader, attachment reader | attached and both standalone forms round trip | attached provenance/Area, peer Via/SeenBy, distribution omissions, SignedOrigin-without-Signature, and wrong-kind tests | I |
| §3.3 FileRequest has Filename, optional Timestamp, RequestIdentifier, and no authentication children | Producer/consumer | `build_file_request`, validator, reader | timestamp-present and timestamp-absent inversions | missing, malformed, extra, path-component, and wrong-kind cases | I |
| §4 Filename has no path component and producers avoid the exact SHOULD-NOT code-point set | Producer/consumer | `validate_produced_filename`, `filename_has_path_component`, `filename_is_portable` | every allowed boundary and representative Unicode name | slash, reverse solidus, and every discouraged code point | I, D |
| §4 Contents is arbitrary bytes without a protocol length limit | Producer/consumer | owned/streaming TLV contents and `Vec<u8>` model | empty, text, and binary contents round trip | enclosing malformed lengths and allocation overflow are rejected by TTS‑0002 framing | I plus dependency evidence |
| §4 RequestIdentifier and TimestampOffset use their exact integer encodings | Producer/consumer | unsigned/signed decoders and builders | all supported width transitions and nonzero offsets | malformed encodings and zero offset | I plus TTS‑0002/TTS‑0007 evidence |
| §4 LegacyAttributes permits only the five persistent bits, excludes bit 4, and omits zero | Producer/consumer/converter | `validate_legacy_attributes` and canonical builders | permitted values and absence round trip | zero, bit 4, and every non-persistent class are refused on production and receipt | I |
| §4 MessageText is empty or U+000A-terminated paragraphs and contains no U+000D | Producer/consumer/converter | `validate_message_text` | empty and terminated paragraphs | U+000D and unterminated final paragraph on production and receipt | I |
| §4 all named text values are UTF‑8 and AreaName has no native legacy restriction | Producer/consumer | `text`, `validate_area` | unrestricted Area Unicode and every optional text field | invalid UTF‑8 at core and suffix boundaries; newline and Control-A prohibitions | I |
| §4 SeenBy is a canonical TTS‑0004 Trimmed Collection; Via and ReplyTo have exact nested grammars | Producer/consumer/forwarder | collection parser/formatter, `push_via`, `read_via`, `read_reply_to` | canonical collections, anonymous key, and raw UTF‑8 suffix round trips | malformed order, missing key/address/timestamp, noncanonical collection, and extra child tests | I plus TTS‑0004 evidence |
| §4 forwarding appends one Via before a new next hop and preserves existing values | Forwarder | `finish_message`, `forward_item` | existing ordinary and anonymous Vias plus local Via survive in order | malformed Via identity, unsupported item, and unsigned forwarding are refused | I |
| §4 Replaces is the exact case-sensitive whole-name Unicode scalar glob | Replaces consumer | `replaces_matches` | literal, `*`, `?`, empty, and multibyte scalar cases | case mismatch, partial match, and one-`?`-per-scalar failures | D |
| §5 Signature covers exactly preceding child bytes and selects Origin before SignedOrigin | Authenticator | `encoded_prefix`, provenance parser, injected/internal verifier cores | all five authentication outcomes, both signer roles, anonymous keys, and exact signed identity | bad signatures, missing keys, invalid key lengths, no fallback, SignedOrigin without Signature, and crypto-operation propagation | I |
| §5 unsigned state, stable identity, and mutable suffix have the defined boundaries | Authenticator/reserializer | `ValidatedItem`, `SignedItemIdentity`, `ItemSigning` | unsigned item, duplicate identity, suffix variation, and attached-File inheritance tests | identity absent for unsigned/invalid/FileRequest; signature mutation invalidates identity | I |
| §6 existing signed items preserve signed bytes and Signature; unsupported representations refuse | Reserializer/converter | `ItemModel`, retained builders, adapter byte-exact self-check | read/re-encode and retained/forwarded round trips | changed signature, absent signer, malformed signed region, and unrepresentable conversion refuse | I plus adapter integration witnesses |
| §6 ordinary legacy input is minted as a new canonical item, not restored authentication | Conversion boundary | adapter origination path and canonical item builders | canonical legacy-to-native Message tests | retained-signature path is separate and refuses a failed self-check | Integration witnesses |

## Conformance-surface manifest

| Source item | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-wire/src/item_format_impl.rs` | `8467d0796d8d66f40bc17826e2bef37f4b39926e69b70ea2ef0d88ef12ee6e3c` | Complete item construction, validation, authentication, identity, reading, retention, and forwarding surface |
| `crates/tith-wire/src/item_format.rs` | `a55c3f65a17f99c5ece9b84e9375c20a463afe939823e5eb34b6d48bb0678942` | Carrier-independent data structures, Filename rules, and Replaces matching |

The first module calls the already-qualified complete TTS‑0002, TTS‑0003,
TTS‑0004, and TTS‑0007 helpers named in their qualification records. The
TTS‑0005 carrier operations are isolated in `item_carrier.rs` and are not part
of this item-format surface.

## Test manifest

| Test file and mapped tests | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-wire/src/item_format_tests.rs`: all 29 `item::tests` | `0b8024c882c9ecaa2da36c4800efea2864aeae2460e66935eb5f6bb735874a2c` | Complete positive and negative item grammar, authentication, identity, retention, and forwarding behavior |
| `crates/tith-wire/src/item_format.rs`: both embedded tests | `a55c3f65a17f99c5ece9b84e9375c20a463afe939823e5eb34b6d48bb0678942` | Exact Filename recommendation and Replaces matching |
| `crates/tith-adapter/src/convert.rs`: canonical reconstruction, retained signing, and unrepresentable-conversion tests | `86dca76b34d6c8464d6d15f8983ac9257ae1e5901c11e4dd6c8fbfc5d9c67bca` | §6 legacy conversion witnesses |
| `crates/tithd/src/accept.rs`, `mail.rs`, and `submission.rs`: item acceptance, relay, transport, duplicate, and submission tests | `9cf6d6096034915b8327c46306228ab27827d658678c43e864074012e75f7f82`, `4f59b9358c40a6d41a5d1e1dd51d5497be88d9e49785c9959084e32fb612b871`, `eded0c1fa9c78ee108f3f411cec403700ab1cdb53ba757d0124934ac761867c8` | Live carrier and persistence integration witnesses |

## Targets, toolchains, and commands

Pinned repository inputs:

| Input | SHA‑256 or exact version |
| --- | --- |
| `Cargo.lock` | `c28e54f12c2b5a8150bfdf2a7555ed13e690b9f7de55b00452fafb8e73c30656` |
| `rust-toolchain.toml` | `da4fa49758e2d8da0f35f79049581134c98b02db0176e515e62367bf9242047c` |
| `.github/workflows/rust.yml` | `05ea7430f172323d474b5dfea8bfb57598a8fbe955d725ff681fa93f58b79ac6` |
| `.github/workflows/qualify-tts-0009.yml` | `e220b7c2e6ec6c31d03ba2dabe08c7489085fcae13e7a4b3473c07069e13cc1c` |
| `qualification/check_source_line_coverage.py` | `a54efa84ebe2e69a1916433e5cf166283b5316cfa862a73a443e0b2bd692d957` |

Normal validation used Rust 1.97.1, compiler commit
`8bab26f4f68e0e26f0bb7960be334d5b520ea452`, with:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

GitHub run
[`32650822745`](https://github.com/RealDeuce/tith/actions/runs/32650822745)
passed these commands and `cargo doc --workspace --no-deps` on Ubuntu 24.04
image `20260816.277.1` and Windows 2025 image `20260818.207.1` at the exact
implementation/test commit. Ubuntu ran 456 tests; Windows ran 455 because one
daemon test is Unix-only. FreeBSD 14.4‑RELEASE‑p3 independently passed the
three required commands and all 456 tests with the same implementation and
test bytes at the evidence commit.

Coverage used Ubuntu image `20260816.277.1`, `nightly-2026-08-20`
(`rustc 1.100.0-nightly`, commit `f7d782a3b`, 2026‑08‑19),
`llvm-tools-preview`, and `cargo-llvm-cov 0.9.0 --locked`. GitHub run
  [`32650822737`](https://github.com/RealDeuce/tith/actions/runs/32650822737)
  ran the complete 456-test workspace before generating and checking the report.

## Results and coverage

| Surface | Functions | Lines | Branches |
| --- | ---: | ---: | ---: |
| `item_format_impl.rs` | 94/94 | 1,387/1,388 aggregate; no uncovered primary source line | 244/244 |
| `item_format.rs` | 10/10 | 89/89 | 18/18 |

LLVM's aggregate line counter reports one more mapped line than covered at the
`include!` boundary, while its annotated `--show-missing-lines` report names no
missing source line and its JSON contains no zero-count primary source segment.
The pinned checker audits those primary mappings and still fails for any real
uncovered source line. This is complete source-line coverage under TPS‑0001
section 6.4, not an exclusion from the conformance surface.

The coverage JSON SHA‑256 is
`5eab8210e592f450c47af6782159e4c0ee6b059da4d77305b350269128200b85`.

## Issue audit

GitHub searches for ASCII and typographic TTS‑0009 and TSP‑0016 names, “Item
Format,” Replaces, AreaName, OriginLine, and Filename found relevant issues
#80, #97, #102–#104, and #112. Their correcting commits closed every
technical issue, including editorial tracker #112. No open
issue alleges a normative defect, ambiguity, security defect, incompatibility,
or external blocker in TTS‑0009 or its dependencies.

## Security review

Untrusted bytes cross the boundary through canonical TLV framing before item
grammar validation. Literal leading children, adjacent anonymous keys,
assigned-type ordering, cardinality, canonical integers and addresses, UTF‑8,
nested lengths, and prohibited combinations are checked before a value is
acted on. Unknown values are retained but never satisfy a known requirement.

SignedData is verified over the exact received child encodings through the
byte before Signature. Origin receives one effective key from the using
context. If that key exists, failure is terminal for that signer and never
falls back to SignedOrigin. SignedOrigin is used only when Origin has no key;
anonymous signers require their adjacent exact key. Cryptographic operation
failures propagate as local errors rather than becoming authentication
results. Tests cover every valid/invalid/unsigned outcome and that failure
propagation.

Stable duplicate identity is exposed only after successful verification and
includes the canonical kind, exact signer identity, and Signature. Mutable
request/routing suffixes are excluded. Forwarding preserves every signed byte
and Signature and only appends suffix data. Embedded Files cannot smuggle an
independent provenance. FileRequests have only transport authentication.

Reserialization retains unknown values and signed bytes exactly. A retained
signature which does not verify under its recorded signer is refused. The
adapter's byte-exact self-check prevents a legacy presentation from silently
becoming a different authenticated item; ordinary legacy input is explicitly
minted as a new canonical item. Filename path components are rejected, while
the exact portability list is producer guidance rather than a consumer
interoperability ban. Replaces policy and candidate selection remain local and
cannot authorize deletion merely by matching a pattern.

## Content-manifest verification

The complete TTS‑0009 revision‑1 evidence is carried from
`qualification/TTS-0009.001.md`, whose digest is recorded above. Revision 2
changes only the four editorial items listed in the complete textual
difference. Its normative body and dependency bytes are unchanged. The
revision‑1 record's TTS‑0002 and TTS‑0007 revision labels were wrong, but its
recorded hashes already named the qualified revision‑3 and revision‑2 bytes
respectively; the corrected labels therefore change no input or result.

Qualifying TTS‑0006 expanded the mapped live transport witness while
preserving all item authentication, carrier, duplicate, and persistence
results. Its current `mail.rs` digest is recorded above. The TTS‑0009-owned
conformance sources, qualified document and dependencies, every other mapped
test, lockfile, toolchain, workflows, and checker remain byte-for-byte
identical.

Pinned qualification run
[`32650822737`](https://github.com/RealDeuce/tith/actions/runs/32650822737)
reran complete TTS‑0009 coverage and all mapped workspace tests at the exact
replacement evidence commit. Normal Rust run
[`32650822745`](https://github.com/RealDeuce/tith/actions/runs/32650822745)
also passed at that commit. No TTS‑0009 normative or conformance result
changed.
