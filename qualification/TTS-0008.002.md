# Qualification: TTS‑0008 revision 2

- Candidate: existing TTS‑0008 revision 2 at `7cdf7c4fc6b78599d5a3edd71bb1400844fb8dc0`
- Publication date: 2026‑08‑23
- Qualified document SHA‑256: `d4aa55939c6b95a6cbef1e7994b6f169c5f98ade8ea872614508babad7dfba8f`
- Evidence commit: `1df1ddd499cdefda55d5164a833db0596e119fab`
- Carried record: `qualification/TTS-0008.001.md`, SHA‑256 `ad2771c59470b612f372ae60147343812585a9e21226e3e7e3567e4a742529c8`
- TPS revision: TPS‑0001 revision 2, SHA‑256 `3e52cba974d025fafc7a0b0dfbe763824c238e14dd7ec0a04f865ec4409c59f6`
- Approval: Stephen Hurd, 2026‑08‑23

TTS‑0008 revision 2 is an editorial revision of the already qualified
revision 1. The repository owner determined that the complete difference below
changes no conforming behavior, wire or file data, assigned value, dependency
ownership, or conformance-test result. The corrected TTS‑0004 citation now
identifies the same qualified revision 5 which the implementation, tests, and
revision‑1 record already used.

## Complete textual difference

These are all changes from TTS‑0008 revision 1 to revision 2:

- `Revision: 1` became `Revision: 2`.
- The publication date changed from 2026‑08‑22 to 2026‑08‑23.
- Section 7 says “this standard” instead of “this proposal”.
- Appendix A added the exact FTA‑1006.002 requirement-keyword reference.
- The TTS‑0004 citation date changed from 2026‑08‑21 to 2026‑08‑22.
- The TTS‑0005 citation date changed from 2026‑08‑22 to 2026‑08‑23.
- The history gained the revision‑2 entry stating that unchanged requirements
  were carried forward with corrected citations and terminology.

No other byte in the document changed.

## Normative dependencies

TTS‑0008 has one internal normative dependency:

| Publication | Revision | SHA‑256 |
| --- | ---: | --- |
| TTS‑0004 | 5 | `abede4c5e0d559ff0dd57967f5e6aefc9aabd380d20ec17924e0fcb63e25a7b6` |

Its external requirement-keyword dependency is FTA‑1006 revision 2,
SHA‑256
`2265e8410311850f4be3068aed446876e017d45d70dd3d41bb3c3991da6f20e4`
for `fta-1006.002`.

TTS‑5000, TSP‑0002, TSP‑0014, and TTS‑0005 are nonnormative examples of
input-channel standards. They own the checks which qualify their results as
evidence and depend one way on this trust model. The resulting technical
dependency graph is `TTS‑0008 → TTS‑0004` and is acyclic.

## Conformance roles and features

| Role | Mandatory features | Optional or conditional features |
| --- | --- | --- |
| Trust service | One durable canonical record, atomic advances, authoritative precedence, and at most one effective key | Authorized initial observation and authenticated continuity are both implemented |
| Evidence-channel consumer | Accept only a qualified category and refuse conflicting, unrelated, or anonymous evidence | Channel-specific provenance beyond the required category is implementation-private |
| Effective-key consumer | Use the one result for every non-anonymous authentication, identity, routing, and authorization decision; never retry another key after failure | Exact anonymous pins are represented by their existing address-and-key identity |
| Rotating operator/server | Retain a predecessor only for a continuity proof and never describe continuity as revocation | The implementation supports retained predecessor secrets and planned successors |

The open-ended permission to retain additional local provenance is
implementation discretion, not a separately named protocol capability. The
reference implementation nevertheless retains the required generic evidence
category and observation time.

## Requirement matrix

`P` is the complete coverage result for `key_pin.rs`, recorded below.

| Requirement | Role | Implementation | Positive evidence | Negative evidence | Coverage |
| --- | --- | --- | --- | --- | --- |
| §2 evidence binds one canonical non-anonymous address to one exact key and one of three categories | Trust service/evidence consumer | typed `Address`, `PublicKey`, `KeyPinEvidence`, `canonical_non_anonymous` | `records_round_trip_and_legacy_directed_provenance_remains_initial_evidence`; authoritative and continuity tests | `initial_observation_requires_an_unanchored_unpinned_non_anonymous_identity` rejects anonymous input | P |
| §2 an input supplies no evidence unless its owning channel checks pass | Evidence consumer | trust store exposes only the already-qualified initial and continuity operations | `trusted_first_contact_pins_then_routes_operational_mail`, `authentication_failure_probes_the_predecessor_and_retries_once` | first-contact configuration and malformed/signature tests in the input-channel implementations prevent calls with unqualified evidence | P plus integration witnesses |
| §2 a self-signed endpoint key requires explicit trust of the exact contact | Evidence consumer | `Trust-On-First-Use` configuration gates `discover_key` | live trusted-first-contact test | `trust_on_first_use_requires_a_non_anonymous_contact_endpoint`; unconfigured peers have no initial-observation path | Integration witnesses |
| §3 retained record has canonical address, current key, anchor, predecessor, time, and latest evidence category | Trust service | `KeyPin`, `encode_key_pin`, `decode_key_pin` | all four `key_pin::tests`, including reopen and exact round trip | round-trip test rejects every truncation, trailing byte, and invalid category | P |
| §3 an advance is atomic and private service state | Trust service | one redb write transaction in `advance`; private `KEY_PINS` table | persistence/reopen test and both continuity tests | rejected predecessor leaves the old record unchanged; corrupt private data fails closed | P |
| §4 an anchor is directly effective unless its exact anchored chain supersedes it | Trust service | `resolve` | `authoritative_publication_restarts_continuity_from_its_current_key` covers A→B→C under anchor A | the same test rejects a proof from A after the authoritative source changes to B | P |
| §4 publication of the successor makes it directly effective; continuity may advance from it again | Trust service | `resolve`, `advance` | the authoritative-publication test changes the anchor to B, then accepts separately authenticated B→C evidence | the preceding A→C attempt is rejected and leaves B effective | P |
| §4 an unrelated new anchor takes precedence; with no anchor a pin is effective; otherwise there is no key | Trust service | `resolve` | authoritative and persistence tests cover all three states | initial-observation test confirms an absent anchor and pin resolves to no key | P |
| §4 every operation uses one effective non-anonymous key | Effective-key consumer | shared `KeyPinStore::resolve` through daemon, router, acceptance, and submission resolvers | live Bundle authentication, hierarchy routing, relay Via, retained signature, duplicate, and authorization tests named in the test manifest | `effective_origin_pin_blocks_signed_origin_for_messages_and_files` and signature-failure tests prove no alternative key or provenance wins | P plus integration witnesses |
| §4 verification failure never retries another key or falls back from Origin to SignedOrigin | Effective-key consumer | wire validation accepts one `KeyResolver` result; submission refuses alternate SignedOrigin | valid effective-pin Message and File paths; continuity retry occurs only after a separately authenticated proof | `origin_key_prevents_signed_origin_fallback`, `effective_origin_pin_blocks_signed_origin_for_messages_and_files`, and unauthenticated-payload tests | Integration witnesses |
| §4 anonymous identities remain exact address/key pairs outside continuity records | Trust service/effective consumer | `canonical_non_anonymous`, `Identity::same_system_as` | anonymous routing and submission identity tests | key-pin test rejects the anonymous address; different anonymous keys compare as different identities | P plus integration witnesses |
| §5 initial observation establishes only with no anchor or pin and logs the decision | Trust service/evidence consumer | `observe_initial`, `initial_trust_message` | initial-observation test and live first-contact test | same test proves an anchor or existing pin returns unchanged; audit-message test checks the exact log text | P plus integration witnesses |
| §5 continuity advances only from the exact effective predecessor and records the transition | Trust service | `advance` | anchored A→B→C and unanchored reopen tests | wrong predecessor under each state returns `KeyPinConflict` and preserves the record | P |
| §5 a newly observed or conflicting key never replaces effective trust | Trust service | unchanged initial result and `KeyPinConflict` | repeated observation of the effective key is unchanged | different repeated observation, unrelated proof, and changed anchor cases retain the effective decision | P |
| §6 a retained predecessor signs only a continuity proof | Rotating server | dedicated `answer_public_key_request` selects current or retained secret; operational traffic uses `local_secret` | `retired_secret_certifies_the_current_key_for_a_dedicated_probe`; live retry test | `a_rotated_local_identity_uses_its_current_key_while_the_nodelist_is_stale` proves ordinary operation uses the current secret | Integration witnesses |
| §6 continuity is not revocation and compromise recovery is external | Rotating operator/software | no revocation state or diagnostic is produced by `advance` | continuity record identifies predecessor and successor without a revocation flag | source and diagnostic audit finds no claim that an advance revokes or proves the predecessor safe | Not executable beyond representation and output audit |

## Conformance-surface manifest

The TTS‑0008-owned state machine and resolution decision are isolated in one
module. The input-channel parsers and the operations which consume its result
own their separate wire, item, routing, and configuration rules; they are
integration witnesses rather than alternate trust implementations.

| Source item | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-store/src/key_pin.rs` (complete file) | `10ac2668fa46cae04410c138b495d83c88a849400d30671fc7c2e7cee76d0691` | §§2–5 evidence categories, durable record, atomic transition, resolution, and failures |

The module uses redb transactions through its stored database handle and
fixed-size `Address`/`PublicKey` values. Those dependencies supply storage,
canonical address, and key types; no TITH-owned helper below this module makes
another trust or precedence decision.

## Test manifest

Tests embedded in a source file use the SHA‑256 digest of that complete file.

| Test file and mapped tests | SHA‑256 | Requirements |
| --- | --- | --- |
| `crates/tith-store/src/key_pin.rs`: all four `key_pin::tests` | `10ac2668fa46cae04410c138b495d83c88a849400d30671fc7c2e7cee76d0691` | §§2–5 complete trust-store behavior |
| `crates/tith-config/src/lib.rs`: `trust_on_first_use_requires_a_non_anonymous_contact_endpoint` | `bde70e65f559fac035d35b14432abbe70f002b8065349bef389afeb27722214f` | §2 exact-contact authorization |
| `crates/tithd/src/deliver.rs`: initial-trust audit-message test and key-resolution/discovery implementation | `779693d4cb22362cdb0f76db45b18e4c7ead657d489d4f4af7a6c9b6315a22b7` | §§2, 4–6 native evidence and effective transport key |
| `crates/tithd/src/mail.rs`: retired-secret, continuity-retry, first-contact, authentication, duplicate, and relay tests named in the matrix | `4f59b9358c40a6d41a5d1e1dd51d5497be88d9e49785c9959084e32fb612b871` | §§2, 4–6 live interoperability |
| `crates/tithd/src/main.rs`: `a_rotated_local_identity_uses_its_current_key_while_the_nodelist_is_stale` | `330b80e19955f1b57ef2bf8468d82518fcd3e4cf42abce3a11a4b3e94dd5e3b3` | §§4 and 6 planned local rotation |
| `crates/tithd/src/submission.rs`: effective-pin, retained-signature, forwarding, and anonymous identity tests named in the matrix | `eded0c1fa9c78ee108f3f411cec403700ab1cdb53ba757d0124934ac761867c8` | §4 item authentication, retained signatures, and identity |
| `crates/tithd/src/accept.rs`: relay Via and signed-item duplicate tests | `9cf6d6096034915b8327c46306228ab27827d658678c43e864074012e75f7f82` | §4 authorization, routing suffix, and duplicate identity |
| `crates/tith-router/src/lib.rs`: all three router tests | `35dcc838697368da7c1f1c20c935c749c264837c3311034e2d8463979aee0a53` | §4 effective-key routing and address-only non-anonymous identity |
| `crates/tith-wire/src/item_format_tests.rs`: `origin_key_prevents_signed_origin_fallback` | `0b8024c882c9ecaa2da36c4800efea2864aeae2460e66935eb5f6bb735874a2c` | §4 no fallback after effective Origin verification failure |

## Targets, toolchains, and commands

Pinned repository inputs:

| Input | SHA‑256 or exact version |
| --- | --- |
| `Cargo.lock` | `b26989f98e1654ada6225ed88520a5f84836cf143fd0240095cd72af0e5b1644` |
| `rust-toolchain.toml` | `da4fa49758e2d8da0f35f79049581134c98b02db0176e515e62367bf9242047c` |
| `.github/workflows/rust.yml` | `05ea7430f172323d474b5dfea8bfb57598a8fbe955d725ff681fa93f58b79ac6` |
| `.github/workflows/qualify-tts-0008.yml` | `895f63ae0f0a43200d0ade69eeecbd83373893ed5ec56b612337e2b874120d30` |
| `qualification/check_source_line_coverage.py` | `a54efa84ebe2e69a1916433e5cf166283b5316cfa862a73a443e0b2bd692d957` |
| GitHub `actions/checkout` | `3d3c42e5aac5ba805825da76410c181273ba90b1` |
| GitHub `dtolnay/rust-toolchain` | `6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772` |

Normal validation used Rust 1.97.1, compiler commit
`8bab26f4f68e0e26f0bb7960be334d5b520ea452`, with rustfmt and Clippy, on:

- `x86_64-unknown-freebsd`, FreeBSD 14.4‑RELEASE‑p3;
- `x86_64-unknown-linux-gnu`, GitHub Ubuntu 24.04 image
  `20260816.277.1`; and
- `x86_64-pc-windows-msvc`, GitHub Windows Server 2025 image
  `20260818.207.1`.

Commands on every normal target:

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

The GitHub targets additionally ran `cargo doc --workspace --no-deps`.

Coverage used Ubuntu image `20260816.277.1`,
`nightly-2026-08-20` (`rustc 1.100.0-nightly`, commit `f7d782a3b`,
2026‑08‑19), `llvm-tools-preview`, and `cargo-llvm-cov 0.9.0 --locked`.
The exact coverage commands were:

```sh
cargo +nightly-2026-08-20 llvm-cov clean --workspace
cargo +nightly-2026-08-20 llvm-cov --branch --workspace --json \
  --output-path tts-0008-coverage.json
cargo +nightly-2026-08-20 llvm-cov report --branch --text \
  --show-missing-lines
python3 qualification/check_source_line_coverage.py \
  tts-0008-coverage.json \
  crates/tith-store/src/key_pin.rs
```

## Results and coverage

- FreeBSD: formatting and Clippy passed; all 484 workspace tests and all doc
  tests passed with the current TTS‑0008 inputs.
- Ubuntu and Windows: GitHub Rust run
  [`32672962993`](https://github.com/RealDeuce/tith/actions/runs/32672962993)
  passed formatting, Clippy, workspace tests, and documentation on both
  targets with the current pinned inputs. Ubuntu ran 484 tests and Windows ran
  483 because one daemon test is Unix-only.
- Coverage: GitHub run
  [`32670992862`](https://github.com/RealDeuce/tith/actions/runs/32670992862)
  passed at the evidence commit and ran all 474 workspace tests before
  producing the report.

The final coverage run established:

| Surface | Functions | Lines | Branches |
| --- | ---: | ---: | ---: |
| `key_pin.rs` | 24/24 | 235/235 | 16/16 |

TPS‑0001 permits complete line or region coverage. The pinned checker uses
complete line coverage because compiler-generated regions at Rust `?`
operators have no independently executable source behavior. Every executable
source line and measured branch is covered.

The coverage JSON SHA‑256 was
`53695b6da77d118bf970d80dfd64bbf2a228277262be5be09362b1eec74b656c`.
The report itself need not remain available: the digest, complete summary,
pinned inputs, and reproduction commands are durable here.

## Issue audit

GitHub searches for ASCII and typographic TTS‑0008 and TSP‑0015 names, “key
pin,” and “continuity” found the relevant standards issues #34, #35, #42,
#49, #50, #66, #79, #83, and #111. Their correcting commits closed every
technical issue, including editorial tracker #111. No open
issue alleges a normative defect, ambiguity, security defect, incompatibility,
or external blocker in TTS‑0008 or TTS‑0004.

## Security review

The trust boundary is the transition from channel-specific authenticated
evidence into the durable key-pin store. The store accepts a typed canonical
non-anonymous address and exact fixed-size public keys; anonymous addresses
are rejected. Input-channel code must authenticate and correlate evidence
before calling it. Native first contact additionally requires explicit policy
for the exact configured contact. A merely self-signed endpoint therefore
cannot create a pin through an ordinary connection.

The record and its transition are one redb transaction. Failed validation,
wrong predecessors, conflicting observations, corrupt records, and crashes
cannot expose a partially advanced current key. The stored anchor and
predecessor make the decision reconstructable after restart. The tests reopen
the database, reject every malformed encoding, and prove rejected updates
leave the prior record effective.

Authoritative state always enters resolution afresh. For A→B→C under anchor
A, C is effective; publication of B makes B directly authoritative, and a
later already-authenticated B→C proof may advance from B again. The trust
engine does not depend on how long a live server retains B. Evidence from A
after B becomes authoritative is rejected, and an unrelated authoritative
key wins over the old chain.

Every non-anonymous key consumer receives the resolver's single result.
Bundle and item verification do not try another key after failure, and an
effective Origin blocks SignedOrigin fallback. Routing hierarchy, relay Via,
authorization, retained signatures, and duplicate identity use the same
result. Anonymous identities remain exact address/key pairs and cannot enter
the non-anonymous continuity store.

A predecessor secret is selected only by the dedicated continuity-probe
reply. Normal traffic and the live local identity use the current secret even
while an authoritative publication is stale. Continuity records contain no
revocation state and no diagnostic claims that the predecessor is safe or
revoked; compromise recovery remains an external trust decision.

## Content-manifest verification

The complete TTS‑0008 revision‑1 evidence is carried from
`qualification/TTS-0008.001.md`, whose digest is recorded above. Revision 2
changes only the seven editorial items listed in the complete textual
difference and leaves the qualified normative body and TTS‑0004 dependency
unchanged.

This replacement records the changed `Cargo.lock` and the current whole-file
digest of the mapped daemon startup witness. That file gained unrelated
service startup behavior without changing its mapped local-key rotation
result. The TTS‑0008-owned `key_pin.rs` conformance source, qualified document
and dependency, every other mapped test, toolchain, targets, workflows,
checker, and configuration remain byte-for-byte identical.

Pinned qualification run
[`32670992862`](https://github.com/RealDeuce/tith/actions/runs/32670992862)
reran complete `key_pin.rs` coverage and every mapped workspace test at the
exact replacement evidence commit. Later changes through the repository
baseline alter none of the recorded TTS‑0008 inputs, and normal Rust run
[`32672962993`](https://github.com/RealDeuce/tith/actions/runs/32672962993)
validated the same implementation and tests. No normative or conformance
result changed.
